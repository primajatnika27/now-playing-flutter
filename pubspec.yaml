name: now_playing
description: Application catalogue Game from RAWG
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  cached_network_image: ^3.2.3
  cool_dropdown: ^2.1.0
  dio: ^5.3.4
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter_any_logo: ^1.1.0
  flutter_html: ^2.2.1
  get_it: ^7.6.0
  google_fonts: ^4.0.4
  intl: ^0.18.1
  provider: ^6.1.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

